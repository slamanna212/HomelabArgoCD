apiVersion: v1
kind: ConfigMap
metadata:
  name: hermes-memory-sidecar-fix
  namespace: hermes
  annotations:
    hermes.nousresearch.com/description: >-
      Durable source and configuration fix for OpenViking recall sidecars. The
      agent patch keeps recalled context in the live request but strips it from
      replayable SQLite/WebUI sidecars. The WebUI entrypoint patch is applied
      after /apptoo is copied into /app and before server.py starts.
data:
  apply_patch.py: |
    #!/usr/bin/env python3
    """Apply exact, fail-closed source patches from a JSON spec.
    
    Usage: apply_patch.py SOURCE_ROOT SPEC_JSON
    """
    from __future__ import annotations
    
    import json
    import os
    import stat
    import sys
    import tempfile
    from pathlib import Path
    
    
    def _atomic_write_text(target: Path, text: str) -> None:
        """Replace one patched source file without exposing a partial write."""
        mode = stat.S_IMODE(target.stat().st_mode)
        temporary: Path | None = None
        try:
            with tempfile.NamedTemporaryFile(
                "w",
                encoding="utf-8",
                dir=target.parent,
                prefix=f".{target.name}.memory-patch-",
                delete=False,
            ) as handle:
                temporary = Path(handle.name)
                handle.write(text)
                handle.flush()
                os.fsync(handle.fileno())
                os.fchmod(handle.fileno(), mode)
            os.replace(temporary, target)
            temporary = None
        finally:
            if temporary is not None:
                try:
                    temporary.unlink()
                except FileNotFoundError:
                    pass
    
    
    def apply_tree(root: Path, spec_path: Path) -> int:
        try:
            root = root.resolve()
            spec = json.loads(spec_path.read_text(encoding="utf-8"))
            hunks = spec["hunks"]
            if not isinstance(hunks, list) or not hunks:
                raise ValueError("spec.hunks must be a non-empty list")
        except Exception as exc:
            print(f"[memory-patch] invalid spec: {exc}", file=sys.stderr, flush=True)
            return 1
    
        failures = 0
        changed: dict[Path, str] = {}
        for index, hunk in enumerate(hunks, 1):
            try:
                rel = Path(hunk["path"])
                old = hunk["old"]
                new = hunk["new"]
                if rel.is_absolute() or ".." in rel.parts:
                    raise ValueError("patch path escapes source root")
                target = (root / rel).resolve()
                target.relative_to(root)
                if not target.is_file():
                    raise FileNotFoundError(rel)
                text = changed.get(target, target.read_text(encoding="utf-8"))
                if new in text:
                    print(f"[memory-patch] already-applied {rel} hunk {index}/{len(hunks)}", flush=True)
                    changed[target] = text
                    continue
                if old not in text:
                    print(f"[memory-patch] MISMATCH {rel} hunk {index}/{len(hunks)}: anchor not found", file=sys.stderr, flush=True)
                    failures += 1
                    continue
                if text.count(old) != 1:
                    print(f"[memory-patch] MISMATCH {rel} hunk {index}/{len(hunks)}: anchor appears {text.count(old)} times", file=sys.stderr, flush=True)
                    failures += 1
                    continue
                changed[target] = text.replace(old, new, 1)
                print(f"[memory-patch] applied {rel} hunk {index}/{len(hunks)}", flush=True)
            except Exception as exc:
                print(f"[memory-patch] MISMATCH hunk {index}/{len(hunks)}: {exc}", file=sys.stderr, flush=True)
                failures += 1
    
        if failures:
            return 2
        for target, text in changed.items():
            if text != target.read_text(encoding="utf-8"):
                _atomic_write_text(target, text)
        return 0
    
    
    def main() -> int:
        if len(sys.argv) != 3:
            print(__doc__, file=sys.stderr)
            return 1
        return apply_tree(Path(sys.argv[1]), Path(sys.argv[2]))
    
    
    if __name__ == "__main__":
        raise SystemExit(main())
  apply_memory_config.py: |
    #!/usr/bin/env python3
    """Idempotently enforce the hot/cold memory contract on the Hermes PVC.

    Hot layer = MEMORY.md/USER.md (always-on rules, injected verbatim, no retrieval).
    Cold layer = OpenViking (everything retrievable). Re-asserted at every boot so a
    runtime edit cannot silently diverge from the declared contract.
    """
    from __future__ import annotations
    
    import os
    import shutil
    import sys
    import tempfile
    from pathlib import Path
    
    from ruamel.yaml import YAML
    
    BOUNDED = {
        # HOT layer ON (hot/cold contract, 2026-09-24). MEMORY.md/USER.md are the
        # verbatim, zero-retrieval layer for always-on rules; OpenViking is the cold
        # layer for everything retrievable. Disabled 2026-09-14 to bound injection
        # while the recall-sidecar bug existed; that bug is fixed by the agent patch
        # in this file, and leaving it off meant the files were still curated weekly
        # and alerted on while being read by nobody.
        # Verify with: grep -c 'MEMORY (your personal notes)' <request dump>.
        "memory_enabled": True,
        "user_profile_enabled": True,
        "provider": "openviking",
        "openviking": {
            "recall_limit": 3,
            "recall_score_threshold": 0.35,
            "recall_max_injected_chars": 1800,
            # 800 elided 19 of 25 profile bullets (head+tail truncation), and the
            # elided middle is where the style/verification rules live; 3000 fits a
            # curated profile whole. Plugin defaults are 6000 / 4.0 / 3.0.
            "profile_token_budget": 3000,
            # 1.5s sits below the store's real latency: 2 of 3 probes timed out and
            # the turn silently received NO recall, logged at DEBUG only.
            "recall_timeout_seconds": 4.0,
            "recall_request_timeout_seconds": 3.0,
            "recall_full_read_limit": 1,
            "recall_prefer_abstract": True,
            "recall_resources": False,
        },
    }
    
    
    def main() -> int:
        if len(sys.argv) != 2:
            print("usage: apply_memory_config.py CONFIG_PATH", file=sys.stderr)
            return 1
        path = Path(sys.argv[1])
        if not path.is_file():
            print(f"[memory-config] missing config: {path}", file=sys.stderr, flush=True)
            return 1
    
        yaml = YAML()
        yaml.preserve_quotes = True
        try:
            with path.open("r", encoding="utf-8") as handle:
                data = yaml.load(handle)
        except Exception as exc:
            print(f"[memory-config] cannot parse config: {type(exc).__name__}", file=sys.stderr, flush=True)
            return 2
        if not isinstance(data, dict):
            print("[memory-config] config root is not a mapping", file=sys.stderr, flush=True)
            return 2
    
        memory = data.get("memory")
        if memory is None:
            memory = {}
            data["memory"] = memory
        if not isinstance(memory, dict):
            print("[memory-config] memory section is not a mapping", file=sys.stderr, flush=True)
            return 2
    
        changed = []
        for key in ("memory_enabled", "user_profile_enabled", "provider"):
            if memory.get(key) != BOUNDED[key]:
                memory[key] = BOUNDED[key]
                changed.append(f"memory.{key}")
        openviking = memory.get("openviking")
        if openviking is None:
            openviking = {}
            memory["openviking"] = openviking
        if not isinstance(openviking, dict):
            print("[memory-config] memory.openviking is not a mapping", file=sys.stderr, flush=True)
            return 2
        for key, value in BOUNDED["openviking"].items():
            if openviking.get(key) != value:
                openviking[key] = value
                changed.append(f"memory.openviking.{key}")
    
        if not changed:
            print("[memory-config] already bounded", flush=True)
            return 0
    
        backup = path.with_name(path.name + ".memory-sidecar-fix.bak")
        try:
            original_stat = path.stat()
            if not backup.exists():
                shutil.copy2(path, backup)
                try:
                    os.chown(backup, original_stat.st_uid, original_stat.st_gid)
                except PermissionError:
                    pass
            mode = original_stat.st_mode & 0o777
            with tempfile.NamedTemporaryFile(
                "w", encoding="utf-8", dir=path.parent, prefix=".memory-config-", delete=False
            ) as handle:
                temporary = Path(handle.name)
                yaml.dump(data, handle)
                handle.flush()
                os.fchmod(handle.fileno(), mode or 0o600)
                try:
                    os.fchown(handle.fileno(), original_stat.st_uid, original_stat.st_gid)
                except PermissionError:
                    pass
            os.replace(temporary, path)
        except Exception as exc:
            try:
                if "temporary" in locals() and temporary.exists():
                    temporary.unlink()
            except OSError:
                pass
            print(f"[memory-config] write failed: {type(exc).__name__}", file=sys.stderr, flush=True)
            return 3
    
        print(f"[memory-config] updated {len(changed)} bounded settings; backup={backup.name}", flush=True)
        return 0
    
    
    if __name__ == "__main__":
        raise SystemExit(main())
  patch_webui_entrypoint.py: |
    #!/usr/bin/env python3
    """Insert the durable WebUI source patch into the upstream init script."""
    from __future__ import annotations
    
    import sys
    from pathlib import Path
    
    MARKER = 'echo ""; echo "== Running hermes-webui"\n'
    INJECTION = '''echo ""; echo "== Applying durable Hermes memory sidecar fix"
    python3 /memory-patches/apply_patch.py /app /memory-patches/webui-spec.json || error_exit "Hermes memory sidecar patch failed"
    
    '''
    
    
    def main() -> int:
        if len(sys.argv) != 2:
            print("usage: patch_webui_entrypoint.py INIT_SCRIPT", file=sys.stderr)
            return 1
        path = Path(sys.argv[1])
        text = path.read_text(encoding="utf-8")
        if INJECTION in text:
            print("[memory-patch] WebUI init wrapper already contains the patch hook", flush=True)
            return 0
        if text.count(MARKER) != 1:
            print("[memory-patch] WebUI init marker missing or not unique", file=sys.stderr, flush=True)
            return 2
        path.write_text(text.replace(MARKER, INJECTION + MARKER, 1), encoding="utf-8")
        print("[memory-patch] inserted WebUI patch hook", flush=True)
        return 0
    
    
    if __name__ == "__main__":
        raise SystemExit(main())
  webui-entrypoint.sh: |
    #!/bin/bash
    set -eu
    umask 077
    
    staged=/app/.hermeswebui_init_memory_sidecar.bash
    cp /hermeswebui_init.bash "$staged"
    chmod 0755 "$staged"
    python3 /memory-patches/patch_webui_entrypoint.py "$staged"
    exec "$staged"
  agent-spec.json: |
    {
      "hunks": [
        {
          "path": "agent/memory_manager.py",
          "old": "    r'<\\s*memory-context\\s*>[\\s\\S]*?</\\s*memory-context\\s*>',\n    re.IGNORECASE,\n)\n_INTERNAL_NOTE_RE = re.compile(\n    r'\\[System note:\\s*The following is recalled memory context,\\s*NOT new user input\\.\\s*Treat as (?:informational background data|authoritative reference data[^\\]]*)\\.\\]\\s*',\n    re.IGNORECASE,\n",
          "new": "    r'<\\s*memory-context\\s*>[\\s\\S]*?</\\s*memory-context\\s*>',\n    re.IGNORECASE,\n)\n_UNTERMINATED_CONTEXT_RE = re.compile(\n    r'<\\s*memory-context\\s*>[\\s\\S]*\\Z',\n    re.IGNORECASE,\n)\n_REPLAY_BLANK_LINES_RE = re.compile(r'\\n[ \\t]*\\n(?:[ \\t]*\\n)+')\n_INTERNAL_NOTE_RE = re.compile(\n    r'\\[System note:\\s*The following is recalled memory context,\\s*NOT new user input\\.\\s*Treat as (?:informational background data|authoritative reference data[^\\]]*)\\.\\]\\s*',\n    re.IGNORECASE,\n"
        },
        {
          "path": "agent/memory_manager.py",
          "old": "    text = _INTERNAL_NOTE_RE.sub('', text)\n    text = _FENCE_TAG_RE.sub('', text)\n    return text\n\n\nclass StreamingContextScrubber:\n",
          "new": "    text = _INTERNAL_NOTE_RE.sub('', text)\n    text = _FENCE_TAG_RE.sub('', text)\n    return text\n\n\ndef strip_memory_context_for_replay(text: Any) -> str:\n    \"\"\"Return text safe to persist and replay without recalled memory context.\"\"\"\n    if not isinstance(text, str):\n        return ''\n    clean = _INTERNAL_CONTEXT_RE.sub('', text)\n    clean = _UNTERMINATED_CONTEXT_RE.sub('', clean)\n    clean = _INTERNAL_NOTE_RE.sub('', clean)\n    clean = _FENCE_TAG_RE.sub('', clean)\n    clean = _REPLAY_BLANK_LINES_RE.sub('\\n\\n', clean)\n    return clean.strip()\n\n\ndef persistable_api_content(api_content: Any, clean_content: Any) -> Optional[str]:\n    \"\"\"Return a replay-safe sidecar, omitting recall-only context.\"\"\"\n    if not isinstance(api_content, str) or not api_content:\n        return None\n    clean = strip_memory_context_for_replay(api_content)\n    if not clean:\n        return None\n    if isinstance(clean_content, str) and clean.strip() == clean_content.strip():\n        return None\n    return clean\n\n\ndef sanitize_replay_value(value: Any) -> Any:\n    \"\"\"Recursively remove recalled context from JSON-like persisted values.\n\n    WebUI sidecars and SQLite rows carry more than the top-level message text:\n    context_messages, reasoning payloads, tool-call arguments, and display\n    metadata can all contain copies of the live API prompt.  Keep the live\n    object untouched and return a sanitized projection for persistence/replay.\n    \"\"\"\n    if isinstance(value, str):\n        return strip_memory_context_for_replay(value)\n    if isinstance(value, dict):\n        return {key: sanitize_replay_value(item) for key, item in value.items()}\n    if isinstance(value, list):\n        return [sanitize_replay_value(item) for item in value]\n    if isinstance(value, tuple):\n        return tuple(sanitize_replay_value(item) for item in value)\n    return value\n\n\nclass StreamingContextScrubber:\n"
        },
        {
          "path": "agent/turn_context.py",
          "old": ")\nfrom agent.context_engine import automatic_compaction_status_message\nfrom agent.iteration_budget import IterationBudget\nfrom agent.memory_manager import build_memory_context_block\nfrom agent.memory_provider import is_trivial_prompt\nfrom agent.message_metadata import append_message, stamp_message_timestamp\nfrom agent.model_metadata import (\n",
          "new": ")\nfrom agent.context_engine import automatic_compaction_status_message\nfrom agent.iteration_budget import IterationBudget\nfrom agent.memory_manager import (\n    build_memory_context_block,\n    persistable_api_content,\n    strip_memory_context_for_replay,\n)\nfrom agent.memory_provider import is_trivial_prompt\nfrom agent.message_metadata import append_message, stamp_message_timestamp\nfrom agent.model_metadata import (\n"
        },
        {
          "path": "agent/turn_context.py",
          "old": "    if not injections:\n        return None\n    return content + \"\\n\\n\" + \"\\n\\n\".join(injections)\n\n\ndef substitute_api_content(api_msg: Dict[str, Any]) -> Optional[str]:\n",
          "new": "    if not injections:\n        return None\n    return content + \"\\n\\n\" + \"\\n\\n\".join(injections)\n\n\ndef compose_persistable_user_api_content(\n    content: str,\n    ext_prefetch_cache: Optional[str],\n    plugin_user_context: Optional[str],\n) -> Optional[str]:\n    \"\"\"Compose the live sidecar, then remove recall-only context for storage.\"\"\"\n    composed = compose_user_api_content(\n        content,\n        ext_prefetch_cache,\n        plugin_user_context,\n    )\n    if composed is None:\n        return None\n    return persistable_api_content(composed, content)\n\n\ndef substitute_api_content(api_msg: Dict[str, Any]) -> Optional[str]:\n"
        },
        {
          "path": "run_agent.py",
          "old": "\n\n# Agent internals extracted to agent/ package for modularity\nfrom agent.memory_manager import sanitize_context\nfrom agent.memory_provider import is_trivial_prompt\nfrom agent.error_classifier import FailoverReason\nfrom agent.redact import redact_sensitive_text\n",
          "new": "\n\n# Agent internals extracted to agent/ package for modularity\nfrom agent.memory_manager import (\n    persistable_api_content,\n    sanitize_context,\n    strip_memory_context_for_replay,\n)\nfrom agent.memory_provider import is_trivial_prompt\nfrom agent.error_classifier import FailoverReason\nfrom agent.redact import redact_sensitive_text\n"
        },
        {
          "path": "run_agent.py",
          "old": "                # they differ from the clean content (stamped by the turn\n                # prologue for prefetch/plugin injections). Written verbatim\n                # so replay can reproduce the sent prefix byte-for-byte.\n                _row_api_content = msg.get(\"api_content\")\n                if not isinstance(_row_api_content, str):\n                    _row_api_content = None\n                _row_timestamp = msg.get(\"timestamp\")\n                # Apply the persist override to THIS row's written values only\n                # (never to the live dict). A multimodal override is a complete\n",
          "new": "                # they differ from the clean content (stamped by the turn\n                # prologue for prefetch/plugin injections). Written verbatim\n                # so replay can reproduce the sent prefix byte-for-byte.\n                _row_api_content = persistable_api_content(\n                    msg.get(\"api_content\"), content\n                )\n                _row_timestamp = msg.get(\"timestamp\")\n                # Apply the persist override to THIS row's written values only\n                # (never to the live dict). A multimodal override is a complete\n"
        },
        {
          "path": "run_agent.py",
          "old": "                # Store the sidecar only when it actually differs.\n                if _row_api_content == content:\n                    _row_api_content = None\n                # Load-time sanitize divergence: get_messages_as_conversation\n                # replays user/assistant rows through\n                # ``sanitize_context(content).strip()``, so content that\n                # sanitize would rewrite (echoed/pasted <memory-context>\n                # fences or system notes) replays different bytes after a\n                # session reload even though THIS turn sent it verbatim.\n                # Capture the sent bytes in the sidecar so a reloaded session\n                # replays what was actually on the wire. Compared in wire form\n                # (both sides .strip()-ed — the api_messages build strips\n                # every outgoing content string) so plain surrounding\n                # whitespace doesn't grow redundant sidecars.\n                if (\n                    _row_api_content is None\n                    and role in (\"user\", \"assistant\")\n                    and isinstance(content, str)\n                    and content\n                    and sanitize_context(content).strip() != content.strip()\n                ):\n                    _row_api_content = content\n                # Persist multimodal tool results as their text summary only —\n                # base64 images would bloat the session DB and aren't useful\n                # for cross-session replay.\n",
          "new": "                # Store the sidecar only when it actually differs.\n                if _row_api_content == content:\n                    _row_api_content = None\n                # Never create a durable copy of recalled memory context from\n                # a load-time sanitization difference. A sidecar may retain only\n                # non-memory text that remains necessary for replay.\n                if (\n                    _row_api_content is None\n                    and role in (\"user\", \"assistant\")\n                    and isinstance(content, str)\n                    and content\n                ):\n                    _replay_content = strip_memory_context_for_replay(content)\n                    if _replay_content != content.strip():\n                        _row_api_content = persistable_api_content(\n                            content, _replay_content\n                        )\n                # Persist multimodal tool results as their text summary only —\n                # base64 images would bloat the session DB and aren't useful\n                # for cross-session replay.\n"
        },
        {
          "path": "hermes_state.py",
          "old": "from contextlib import contextmanager\nfrom pathlib import Path\n\nfrom agent.memory_manager import sanitize_context\nfrom agent.session_activity import ActivityProvenance\nfrom agent.message_sanitization import _sanitize_surrogates\n# Intrinsic persistence marker stamped on message dicts that are known-durable\n",
          "new": "from contextlib import contextmanager\nfrom pathlib import Path\n\nfrom agent.memory_manager import (\n    persistable_api_content,\n    sanitize_context,\n    sanitize_replay_value,\n    strip_memory_context_for_replay,\n)\nfrom agent.session_activity import ActivityProvenance\nfrom agent.message_sanitization import _sanitize_surrogates\n# Intrinsic persistence marker stamped on message dicts that are known-durable\n"
        },
        {
          "path": "hermes_state.py",
          "old": "        wire bytes).\n        \"\"\"\n        # Display metadata is presentation-only and never changes the model\n        # context role/content replayed to providers.\n        display_metadata_json = self._encode_display_metadata(display_metadata)\n        # Serialize structured fields to JSON before entering the write txn\n        reasoning_details_json = self._reasoning_json_text(reasoning_details)\n        codex_items_json = self._reasoning_json_text(codex_reasoning_items)\n        codex_message_items_json = self._reasoning_json_text(codex_message_items)\n        # tool_calls may arrive as a Python list (from the live agent) or\n        # as a JSON string (from import/export). Parse first to avoid\n        # double-encoding.\n        if isinstance(tool_calls, str):\n            try:\n                tool_calls = json.loads(tool_calls)\n            except (json.JSONDecodeError, TypeError):\n                tool_calls = []\n        tool_calls_json = json.dumps(tool_calls) if tool_calls else None\n        # Multimodal content (list of parts) must be JSON-encoded: sqlite3\n        # cannot bind list/dict parameters directly.\n        stored_content = self._encode_content(content)\n\n        message_timestamp = time.time()\n        if timestamp is not None:\n",
          "new": "        wire bytes).\n        \"\"\"\n        # Display metadata is presentation-only and never changes the model\n        # context role/content replayed to providers. Sanitize its JSON values\n        # before persistence so recalled context cannot hide in a sidecar.\n        display_metadata_json = self._encode_display_metadata(\n            sanitize_replay_value(display_metadata)\n        )\n        # Serialize structured fields to JSON before entering the write txn\n        persisted_reasoning = sanitize_replay_value(reasoning)\n        persisted_reasoning_content = sanitize_replay_value(reasoning_content)\n        reasoning_details_json = self._reasoning_json_text(\n            sanitize_replay_value(reasoning_details)\n        )\n        codex_items_json = self._reasoning_json_text(\n            sanitize_replay_value(codex_reasoning_items)\n        )\n        codex_message_items_json = self._reasoning_json_text(\n            sanitize_replay_value(codex_message_items)\n        )\n        # tool_calls may arrive as a Python list (from the live agent) or\n        # as a JSON string (from import/export). Parse first to avoid\n        # double-encoding, then sanitize nested argument values.\n        if isinstance(tool_calls, str):\n            try:\n                tool_calls = json.loads(tool_calls)\n            except (json.JSONDecodeError, TypeError):\n                tool_calls = []\n        tool_calls = sanitize_replay_value(tool_calls)\n        tool_calls_json = json.dumps(tool_calls) if tool_calls else None\n        # Multimodal content (list of parts) must be JSON-encoded: sqlite3\n        # cannot bind list/dict parameters directly. Store a sanitized\n        # projection; the live object is never mutated.\n        persisted_content = sanitize_replay_value(content)\n        stored_content = self._encode_content(persisted_content)\n        stored_api_content = persistable_api_content(api_content, persisted_content)\n\n        message_timestamp = time.time()\n        if timestamp is not None:\n"
        },
        {
          "path": "hermes_state.py",
          "old": "                    message_timestamp,\n                    token_count,\n                    finish_reason,\n                    _scrub_surrogates(reasoning),\n                    _scrub_surrogates(reasoning_content),\n                    reasoning_details_json,\n                    codex_items_json,\n                    codex_message_items_json,\n",
          "new": "                    message_timestamp,\n                    token_count,\n                    finish_reason,\n                    _scrub_surrogates(persisted_reasoning)\n                    if isinstance(persisted_reasoning, str)\n                    else None,\n                    _scrub_surrogates(persisted_reasoning_content)\n                    if isinstance(persisted_reasoning_content, str)\n                    else None,\n                    reasoning_details_json,\n                    codex_items_json,\n                    codex_message_items_json,\n"
        },
        {
          "path": "hermes_state.py",
          "old": "                    1 if observed else 0,\n                    1 if _compressed_summary else 0,\n                    1,\n                    _scrub_surrogates(api_content) if isinstance(api_content, str) else None,\n                    _scrub_surrogates(display_kind) if isinstance(display_kind, str) else None,\n                    display_metadata_json,\n                ),\n",
          "new": "                    1 if observed else 0,\n                    1 if _compressed_summary else 0,\n                    1,\n                    _scrub_surrogates(stored_api_content)\n                    if isinstance(stored_api_content, str)\n                    else None,\n                    _scrub_surrogates(display_kind) if isinstance(display_kind, str) else None,\n                    display_metadata_json,\n                ),\n"
        },
        {
          "path": "hermes_state.py",
          "old": "                        message_timestamp = float(ts_value)\n                except (TypeError, ValueError):\n                    logger.debug(\"Ignoring invalid explicit message timestamp: %r\", msg.get(\"timestamp\"))\n            reasoning_details = msg.get(\"reasoning_details\") if role == \"assistant\" else None\n            codex_reasoning_items = (\n                msg.get(\"codex_reasoning_items\") if role == \"assistant\" else None\n            )\n            codex_message_items = (\n                msg.get(\"codex_message_items\") if role == \"assistant\" else None\n            )\n            reasoning_details_json = self._reasoning_json_text(reasoning_details)\n            codex_items_json = self._reasoning_json_text(codex_reasoning_items)\n",
          "new": "                        message_timestamp = float(ts_value)\n                except (TypeError, ValueError):\n                    logger.debug(\"Ignoring invalid explicit message timestamp: %r\", msg.get(\"timestamp\"))\n            content = sanitize_replay_value(msg.get(\"content\"))\n            reasoning = (\n                sanitize_replay_value(msg.get(\"reasoning\"))\n                if role == \"assistant\" else None\n            )\n            reasoning_content = (\n                sanitize_replay_value(msg.get(\"reasoning_content\"))\n                if role == \"assistant\" else None\n            )\n            reasoning_details = (\n                sanitize_replay_value(msg.get(\"reasoning_details\"))\n                if role == \"assistant\" else None\n            )\n            codex_reasoning_items = (\n                sanitize_replay_value(msg.get(\"codex_reasoning_items\"))\n                if role == \"assistant\" else None\n            )\n            codex_message_items = (\n                sanitize_replay_value(msg.get(\"codex_message_items\"))\n                if role == \"assistant\" else None\n            )\n            reasoning_details_json = self._reasoning_json_text(reasoning_details)\n            codex_items_json = self._reasoning_json_text(codex_reasoning_items)\n"
        },
        {
          "path": "hermes_state.py",
          "old": "            # tool_calls may arrive as a Python list (from the live agent)\n            # or as a JSON string (from import_sessions / export_session,\n            # which store it as TEXT). json.dumps on an already-serialized\n            # string double-encodes it, so parse first.\n            if isinstance(tool_calls, str):\n                try:\n                    tool_calls = json.loads(tool_calls)\n                except (json.JSONDecodeError, TypeError):\n                    tool_calls = []\n            tool_calls_json = json.dumps(tool_calls) if tool_calls else None\n            # Accept either `platform_message_id` (new explicit name) or\n            # `message_id` (yuanbao's existing convention on message dicts).\n",
          "new": "            # tool_calls may arrive as a Python list (from the live agent)\n            # or as a JSON string (from import_sessions / export_session,\n            # which store it as TEXT). json.dumps on an already-serialized\n            # string double-encodes it, so parse first, then sanitize nested\n            # argument values.\n            if isinstance(tool_calls, str):\n                try:\n                    tool_calls = json.loads(tool_calls)\n                except (json.JSONDecodeError, TypeError):\n                    tool_calls = []\n            tool_calls = sanitize_replay_value(tool_calls)\n            tool_calls_json = json.dumps(tool_calls) if tool_calls else None\n            # Accept either `platform_message_id` (new explicit name) or\n            # `message_id` (yuanbao's existing convention on message dicts).\n"
        },
        {
          "path": "hermes_state.py",
          "old": "                msg.get(\"platform_message_id\") or msg.get(\"message_id\")\n            )\n\n            api_content = msg.get(\"api_content\")\n\n            cur = conn.execute(\n                \"\"\"INSERT INTO messages (session_id, role, content, tool_call_id,\n",
          "new": "                msg.get(\"platform_message_id\") or msg.get(\"message_id\")\n            )\n\n            api_content = persistable_api_content(\n                msg.get(\"api_content\"), content\n            )\n\n            cur = conn.execute(\n                \"\"\"INSERT INTO messages (session_id, role, content, tool_call_id,\n"
        },
        {
          "path": "hermes_state.py",
          "old": "                (\n                    session_id,\n                    role,\n                    self._encode_content(msg.get(\"content\")),\n                    msg.get(\"tool_call_id\"),\n                    tool_calls_json,\n                    _scrub_surrogates(msg.get(\"tool_name\")),\n                    msg.get(\"effect_disposition\"),\n                    message_timestamp,\n                    msg.get(\"token_count\"),\n                    msg.get(\"finish_reason\"),\n                    _scrub_surrogates(msg.get(\"reasoning\")) if role == \"assistant\" else None,\n                    _scrub_surrogates(msg.get(\"reasoning_content\")) if role == \"assistant\" else None,\n                    reasoning_details_json,\n                    codex_items_json,\n                    codex_message_items_json,\n",
          "new": "                (\n                    session_id,\n                    role,\n                    self._encode_content(content),\n                    msg.get(\"tool_call_id\"),\n                    tool_calls_json,\n                    _scrub_surrogates(\n                        sanitize_replay_value(msg.get(\"tool_name\"))\n                    ) if isinstance(msg.get(\"tool_name\"), str) else None,\n                    msg.get(\"effect_disposition\"),\n                    message_timestamp,\n                    msg.get(\"token_count\"),\n                    msg.get(\"finish_reason\"),\n                    _scrub_surrogates(reasoning) if isinstance(reasoning, str) else None,\n                    _scrub_surrogates(reasoning_content)\n                    if isinstance(reasoning_content, str)\n                    else None,\n                    reasoning_details_json,\n                    codex_items_json,\n                    codex_message_items_json,\n"
        },
        {
          "path": "hermes_state.py",
          "old": "                    1,\n                    _scrub_surrogates(api_content) if isinstance(api_content, str) else None,\n                    _scrub_surrogates(msg.get(\"display_kind\")) if isinstance(msg.get(\"display_kind\"), str) else None,\n                    self._encode_display_metadata(msg.get(\"display_metadata\")),\n                ),\n            )\n            if isinstance(msg, dict) and cur.lastrowid is not None:\n",
          "new": "                    1,\n                    _scrub_surrogates(api_content) if isinstance(api_content, str) else None,\n                    _scrub_surrogates(msg.get(\"display_kind\")) if isinstance(msg.get(\"display_kind\"), str) else None,\n                    self._encode_display_metadata(\n                        sanitize_replay_value(msg.get(\"display_metadata\"))\n                    ),\n                ),\n            )\n            if isinstance(msg, dict) and cur.lastrowid is not None:\n"
        },
        {
          "path": "hermes_state.py",
          "old": "        (0 or 1).\n        \"\"\"\n        encoded = self._encode_content(content)\n\n        def _do(conn):\n            cursor = conn.execute(\n",
          "new": "        (0 or 1).\n        \"\"\"\n        encoded = self._encode_content(content)\n        persisted_api_content = persistable_api_content(api_content, content)\n\n        def _do(conn):\n            cursor = conn.execute(\n"
        },
        {
          "path": "hermes_state.py",
          "old": "                \"WHERE session_id = ? AND role = 'user' AND active = 1 \"\n                \"ORDER BY id DESC LIMIT 1\"\n                \") AND content IS ?\",\n                (_scrub_surrogates(api_content), session_id, encoded),\n            )\n            return cursor.rowcount\n\n",
          "new": "                \"WHERE session_id = ? AND role = 'user' AND active = 1 \"\n                \"ORDER BY id DESC LIMIT 1\"\n                \") AND content IS ?\",\n                (\n                    _scrub_surrogates(persisted_api_content),\n                    session_id,\n                    encoded,\n                ),\n            )\n            return cursor.rowcount\n\n"
        },
        {
          "path": "hermes_state.py",
          "old": "        # whole accumulated lineage for every user row.\n        exact_user_clones: Dict[Tuple[Any, str], Dict[str, Any]] = {}\n        for row in rows:\n            content = self._decode_content(row[\"content\"])\n            if row[\"role\"] in {\"user\", \"assistant\"} and isinstance(content, str):\n                content = sanitize_context(content).strip()\n            msg = {\"role\": row[\"role\"], \"content\": content}\n            # Born durable (#92231): this dict is materialized FROM a durable\n            # row, so stamp the persistence marker at the source instead of\n",
          "new": "        # whole accumulated lineage for every user row.\n        exact_user_clones: Dict[Tuple[Any, str], Dict[str, Any]] = {}\n        for row in rows:\n            content = sanitize_replay_value(self._decode_content(row[\"content\"]))\n            msg = {\"role\": row[\"role\"], \"content\": content}\n            # Born durable (#92231): this dict is materialized FROM a durable\n            # row, so stamp the persistence marker at the source instead of\n"
        },
        {
          "path": "hermes_state.py",
          "old": "            # strips it before the wire.\n            if include_row_ids and row[\"id\"] is not None:\n                msg[\"_row_id\"] = row[\"id\"]\n            # api_content is the byte-fidelity sidecar: the exact string sent\n            # to the API when it differed from the clean content. Returned\n            # VERBATIM — no sanitize_context, no strip — because the replay\n            # path substitutes it for content to keep the provider prompt\n            # cache prefix byte-stable across turns. Cleaning it here would\n            # re-introduce the divergence it exists to remove.\n            if row[\"api_content\"]:\n                msg[\"api_content\"] = row[\"api_content\"]\n            if row[\"display_kind\"]:\n                msg[\"display_kind\"] = row[\"display_kind\"]\n            if row[\"display_metadata\"]:\n                decoded = self._decode_display_metadata(row[\"display_metadata\"])\n                if decoded is not None:\n                    msg[\"display_metadata\"] = decoded\n            if include_summary_markers and row[\"_compressed_summary\"]:\n                msg[\"_compressed_summary\"] = True\n            if row[\"timestamp\"]:\n",
          "new": "            # strips it before the wire.\n            if include_row_ids and row[\"id\"] is not None:\n                msg[\"_row_id\"] = row[\"id\"]\n            # The live API may receive recalled memory in this sidecar, but\n            # durable replay must never re-inject it. Preserve only unrelated\n            # sidecar text that still differs from the clean message content.\n            replay_api_content = persistable_api_content(\n                row[\"api_content\"], content\n            )\n            if replay_api_content:\n                msg[\"api_content\"] = replay_api_content\n            if row[\"display_kind\"]:\n                msg[\"display_kind\"] = row[\"display_kind\"]\n            if row[\"display_metadata\"]:\n                decoded = self._decode_display_metadata(row[\"display_metadata\"])\n                if decoded is not None:\n                    msg[\"display_metadata\"] = sanitize_replay_value(decoded)\n            if include_summary_markers and row[\"_compressed_summary\"]:\n                msg[\"_compressed_summary\"] = True\n            if row[\"timestamp\"]:\n"
        },
        {
          "path": "hermes_state.py",
          "old": "                msg[\"effect_disposition\"] = row[\"effect_disposition\"]\n            if row[\"tool_calls\"]:\n                try:\n                    msg[\"tool_calls\"] = json.loads(row[\"tool_calls\"])\n                except (json.JSONDecodeError, TypeError):\n                    logger.warning(\"Failed to deserialize tool_calls in conversation replay, falling back to []\")\n                    msg[\"tool_calls\"] = []\n",
          "new": "                msg[\"effect_disposition\"] = row[\"effect_disposition\"]\n            if row[\"tool_calls\"]:\n                try:\n                    msg[\"tool_calls\"] = sanitize_replay_value(json.loads(row[\"tool_calls\"]))\n                except (json.JSONDecodeError, TypeError):\n                    logger.warning(\"Failed to deserialize tool_calls in conversation replay, falling back to []\")\n                    msg[\"tool_calls\"] = []\n"
        },
        {
          "path": "hermes_state.py",
          "old": "                if row[\"finish_reason\"]:\n                    msg[\"finish_reason\"] = row[\"finish_reason\"]\n                if row[\"reasoning\"]:\n                    msg[\"reasoning\"] = row[\"reasoning\"]\n                if row[\"reasoning_content\"] is not None:\n                    msg[\"reasoning_content\"] = row[\"reasoning_content\"]\n                if row[\"reasoning_details\"]:\n                    try:\n                        msg[\"reasoning_details\"] = json.loads(row[\"reasoning_details\"])\n                    except (json.JSONDecodeError, TypeError):\n                        logger.warning(\"Failed to deserialize reasoning_details, falling back to None\")\n                        msg[\"reasoning_details\"] = None\n                if row[\"codex_reasoning_items\"]:\n                    try:\n                        msg[\"codex_reasoning_items\"] = json.loads(row[\"codex_reasoning_items\"])\n                    except (json.JSONDecodeError, TypeError):\n                        logger.warning(\"Failed to deserialize codex_reasoning_items, falling back to None\")\n                        msg[\"codex_reasoning_items\"] = None\n                if row[\"codex_message_items\"]:\n                    try:\n                        msg[\"codex_message_items\"] = json.loads(row[\"codex_message_items\"])\n                    except (json.JSONDecodeError, TypeError):\n                        logger.warning(\"Failed to deserialize codex_message_items, falling back to None\")\n                        msg[\"codex_message_items\"] = None\n",
          "new": "                if row[\"finish_reason\"]:\n                    msg[\"finish_reason\"] = row[\"finish_reason\"]\n                if row[\"reasoning\"]:\n                    msg[\"reasoning\"] = strip_memory_context_for_replay(row[\"reasoning\"])\n                if row[\"reasoning_content\"] is not None:\n                    msg[\"reasoning_content\"] = strip_memory_context_for_replay(\n                        row[\"reasoning_content\"]\n                    )\n                if row[\"reasoning_details\"]:\n                    try:\n                        msg[\"reasoning_details\"] = sanitize_replay_value(\n                            json.loads(row[\"reasoning_details\"])\n                        )\n                    except (json.JSONDecodeError, TypeError):\n                        logger.warning(\"Failed to deserialize reasoning_details, falling back to None\")\n                        msg[\"reasoning_details\"] = None\n                if row[\"codex_reasoning_items\"]:\n                    try:\n                        msg[\"codex_reasoning_items\"] = sanitize_replay_value(\n                            json.loads(row[\"codex_reasoning_items\"])\n                        )\n                    except (json.JSONDecodeError, TypeError):\n                        logger.warning(\"Failed to deserialize codex_reasoning_items, falling back to None\")\n                        msg[\"codex_reasoning_items\"] = None\n                if row[\"codex_message_items\"]:\n                    try:\n                        msg[\"codex_message_items\"] = sanitize_replay_value(\n                            json.loads(row[\"codex_message_items\"])\n                        )\n                    except (json.JSONDecodeError, TypeError):\n                        logger.warning(\"Failed to deserialize codex_message_items, falling back to None\")\n                        msg[\"codex_message_items\"] = None\n"
        }
      ]
    }
  webui-spec.json: |
    {
      "hunks": [
        {
          "path": "api/models.py",
          "old": "logger = logging.getLogger(__name__)\nCLI_VISIBLE_SESSION_LIMIT = 20\n",
          "new": "logger = logging.getLogger(__name__)\n\n\ndef replay_safe_messages(messages):\n    \"\"\"Copy messages for WebUI persistence without recalled memory context.\"\"\"\n    from agent.memory_manager import persistable_api_content, sanitize_replay_value\n\n    safe = []\n    for message in messages or []:\n        if not isinstance(message, dict):\n            safe.append(copy.deepcopy(message))\n            continue\n        projected = sanitize_replay_value(copy.deepcopy(message))\n        if \"api_content\" in projected:\n            projected[\"api_content\"] = persistable_api_content(\n                projected.get(\"api_content\"), projected.get(\"content\")\n            )\n        safe.append(projected)\n    return safe\n\n\nCLI_VISIBLE_SESSION_LIMIT = 20\n"
        },
        {
          "path": "api/models.py",
          "old": "        ]\n        meta = {k: getattr(self, k, None) for k in METADATA_FIELDS}\n        # #5854: message_count and a compact anchor-scene fingerprint go in the\n        # metadata prefix (BEFORE messages) so load_metadata_only() and the\n        # sidebar-poll freshness check never have to parse the full (250-480KB)\n        # scene bodies. message_count is placed BEFORE anchor_scene_index so a\n        # legacy-format reader that stops at a scene key still finds the count.\n        # The full anchor_activity_scenes bodies serialize AFTER messages.\n        meta['message_count'] = len(self.messages or [])\n        meta['anchor_scene_index'] = _anchor_scene_index_from_records(self.anchor_activity_scenes)\n        # Keep the in-memory fingerprint aligned with what we just persisted, so a\n        # later metadata-only reload of THIS object (or any fingerprint reader)\n        # sees the current value rather than a stale load-time snapshot (#5854\n        # defense-in-depth; the cached-side freshness check reads real records,\n        # not this, so this is belt-and-suspenders).\n        self._anchor_scene_index = dict(meta['anchor_scene_index'])\n        meta['messages'] = self.messages\n        meta['tool_calls'] = self.tool_calls\n        meta['anchor_activity_scenes'] = self.anchor_activity_scenes if isinstance(self.anchor_activity_scenes, dict) else {}\n        # Fields not in METADATA_FIELDS (e.g. last_usage) go at the end. Exclude\n        # the keys we placed explicitly above so they aren't emitted twice.\n        _placed = {'message_count', 'anchor_scene_index', 'messages', 'tool_calls', 'anchor_activity_scenes'}\n        extra = {k: v for k, v in self.__dict__.items()\n                 if k not in METADATA_FIELDS and k not in _placed\n                 and not k.startswith('_')}\n        payload = json.dumps({**meta, **extra}, ensure_ascii=False, indent=2)\n",
          "new": "        ]\n        from agent.memory_manager import sanitize_replay_value\n\n        meta = {\n            k: sanitize_replay_value(getattr(self, k, None))\n            for k in METADATA_FIELDS\n        }\n        # #5854: message_count and a compact anchor-scene fingerprint go in the\n        # metadata prefix (BEFORE messages) so load_metadata_only() and the\n        # sidebar-poll freshness check never have to parse the full (250-480KB)\n        # scene bodies. message_count is placed BEFORE anchor_scene_index so a\n        # legacy-format reader that stops at a scene key still finds the count.\n        # The full anchor_activity_scenes bodies serialize AFTER messages.\n        meta['message_count'] = len(self.messages or [])\n        meta['anchor_scene_index'] = _anchor_scene_index_from_records(self.anchor_activity_scenes)\n        # Keep the in-memory fingerprint aligned with what we just persisted, so a\n        # later metadata-only reload of THIS object (or any fingerprint reader)\n        # sees the current value rather than a stale load-time snapshot (#5854\n        # defense-in-depth; the cached-side freshness check reads real records,\n        # not this, so this is belt-and-suspenders).\n        self._anchor_scene_index = dict(meta['anchor_scene_index'])\n        meta['messages'] = replay_safe_messages(self.messages)\n        meta['context_messages'] = replay_safe_messages(self.context_messages)\n        meta['tool_calls'] = sanitize_replay_value(copy.deepcopy(self.tool_calls))\n        meta['anchor_activity_scenes'] = sanitize_replay_value(\n            copy.deepcopy(self.anchor_activity_scenes)\n            if isinstance(self.anchor_activity_scenes, dict)\n            else {}\n        )\n        # Fields not in METADATA_FIELDS (e.g. last_usage) go at the end. Exclude\n        # the keys we placed explicitly above so they aren't emitted twice.\n        _placed = {\n            'message_count', 'anchor_scene_index', 'messages', 'context_messages',\n            'tool_calls', 'anchor_activity_scenes',\n        }\n        extra = {\n            k: sanitize_replay_value(v)\n            for k, v in self.__dict__.items()\n            if k not in METADATA_FIELDS and k not in _placed\n            and not k.startswith('_')\n        }\n        payload = json.dumps({**meta, **extra}, ensure_ascii=False, indent=2)\n"
        }
      ]
    }
