apiVersion: v1
kind: Service
metadata:
  name: ai-usage-exporter
  namespace: ai-monitoring
  labels:
    app: ai-usage-exporter
    component: metrics
spec:
  selector:
    app: ai-usage-exporter
    component: metrics
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
