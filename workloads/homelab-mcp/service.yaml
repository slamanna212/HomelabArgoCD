apiVersion: v1
kind: Service
metadata:
  name: homelab-mcp
  namespace: homelab-mcp
  labels:
    app: homelab-mcp
spec:
  type: ClusterIP
  selector:
    app: homelab-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
