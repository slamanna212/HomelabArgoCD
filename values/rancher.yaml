hostname: rancher.local

replicas: 1

# Traefik handles external traffic; Rancher serves HTTP internally
tls:
  source: external

# Disable the built-in ingress since we use Traefik IngressRoute
ingress:
  enabled: false
