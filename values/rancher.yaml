hostname: rancher.local

replicas: 1

# Rancher manages its own TLS; Traefik passes through via TCP
tls: rancher

# Disable the built-in ingress since we use Traefik IngressRoute
ingress:
  enabled: false
