image:
  repository: ghcr.io/slamanna212/matchexec
  tag: latest

persistence:
  storageClass: longhorn
  size: 1Gi

metrics:
  serviceMonitor:
    enabled: true
    labels:
      release: kube-prometheus-stack
  grafana:
    dashboardConfigMap: true
