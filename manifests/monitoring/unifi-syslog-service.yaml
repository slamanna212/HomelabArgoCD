apiVersion: v1
kind: Service
metadata:
  name: unifi-syslog
  namespace: monitoring
  annotations:
    metallb.universe.tf/loadBalancerIPs: 10.1.20.203
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: promtail
  ports:
    - name: syslog-udp
      port: 1514
      targetPort: 1514
      protocol: UDP
