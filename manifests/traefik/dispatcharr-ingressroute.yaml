apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: dispatcharr-ui
  namespace: traefik
spec:
  entryPoints:
    - dispatcharr
  routes:
    - match: HostSNI(`*`)
      services:
        - name: dispatcharr
          namespace: dispatcharr
          port: 9191
