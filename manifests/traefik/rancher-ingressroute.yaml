apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: rancher-ui
  namespace: traefik
spec:
  entryPoints:
    - rancher
  routes:
    - match: HostSNI(`*`)
      services:
        - name: rancher
          namespace: cattle-system
          port: 443
  tls:
    passthrough: true
