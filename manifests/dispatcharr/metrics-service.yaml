apiVersion: v1
kind: Service
metadata:
  name: dispatcharr-metrics
  namespace: dispatcharr
  labels:
    app: dispatcharr
    component: metrics
spec:
  selector:
    app: dispatcharr
    component: web
  ports:
    - name: metrics
      port: 9192
      targetPort: 9192
