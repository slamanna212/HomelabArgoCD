apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: plex-exporter
  namespace: media-monitoring
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: plex-exporter
      component: metrics
  namespaceSelector:
    matchNames:
      - media-monitoring
  endpoints:
    - port: metrics
      path: /metrics
